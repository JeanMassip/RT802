version: '3'
services:
  ca:
     build: ./CertificateAuthority
  mosquitto:
    image: eclipse-mosquitto
    volumes:
     - ./config/:/mosquitto/config/:rw
    ports:
     - 1883:1883
     - 9001:9001
  broker:
    build: ./Broker
    depends_on:
      - mosquitto
      - ca
  vehicule1:
    build: ./Vehicules
    environment:
      - Password=DtIflMN1M@nLJ_wSoHY~
      - MAX_MESSAGES=50
      - SLOW_START=20
      - SLOW_STOP=40
      - BROKER_ADDR="mosquitto"
      - BROKER_PORT=1883
    depends_on:
      - ca
  vehicule2:
    build: ./Vehicules
    environment:
      - Password=DtIflMN1M@nLJ_wSoHY~
      - MAX_MESSAGES=50
      - SLOW_START=20
      - SLOW_STOP=40
      - BROKER_ADDR="mosquitto"
      - BROKER_PORT=1883
    depends_on:
      - ca
